"use strict";
/*
//first switch example

let condition = +prompt("Enter a number:", "");

switch (condition) {
    case 3:
        alert("Too small");
        break;
    case 4: 
        alert("Done");
        break;
    case 5:
        alert("Too big");
        break;
    default:
        alert("I don't know how much can be this value");
}

//Task, switch to if

let browser = prompt("What browser are you using?");
if (browser) {
    if (browser == "Edge"){
        alert("Your've got the Edge!")
    } else if (browser == "Firefox" || browser == "Chrome" || browser == "Safari" || browser == "Opera"){
        alert("Okay we support these browser too");
    } else {
        alert("We hope that this page looks ok!")
    }
}

//Task, if to switch

let a = +prompt("a?", "");

switch (a) {
    case 0: 
        alert(0);
        break;
    case 1:
        alert(1);
        break;
    case 2: 
    case 3:
        alert('2 or 3');
        break;
}*/