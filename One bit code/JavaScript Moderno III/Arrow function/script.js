function normal_sum(first_value, second_value) {
  return first_value + second_value;
}
console.log(`Normal sum : ${normal_sum(2, 2)}`);

const arrow_sum = (first_value, second_value) => first_value + second_value;
console.log(`Arrow sum : ${arrow_sum(2, 2)}`);
